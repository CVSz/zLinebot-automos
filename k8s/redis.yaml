apiVersion: apps/v1
kind: Deployment
metadata:
  name: zlinebot-automos-redis
spec:
  selector:
    matchLabels:
      app: zlinebot-automos-redis
  template:
    metadata:
      labels:
        app: zlinebot-automos-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: zlinebot-automos-redis
  ports:
    - port: 6379
      targetPort: 6379
